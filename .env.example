NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
NEXT_PUBLIC_MAPBOX_TOKEN=
# Used to build absolute URLs in the sitemap and Open Graph tags — set this
# to the real deployed domain in production (e.g. https://garagehunt.ca).
NEXT_PUBLIC_SITE_URL=http://localhost:3000
